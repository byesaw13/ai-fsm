node_modules/
.pnpm-store/
.next/
dist/
coverage/
.env
.env.*
!.env.example
apps/*/.env.local
*.log
.DS_Store
.vscode/
.idea/
tsconfig.tsbuildinfo

# Playwright output
test-results/
playwright-report/

# Research docs
docs/*.pdf
docs/deep-research-report.md
docs/research

# Local data tooling — personal purchase exports + one-off import scripts
# (not app features; keep business data and ad-hoc scripts out of the repo)
docs/Purchase_History_*.csv
scripts/import-expenses.js

# impeccable (design skill) — the root .impeccable/ is the skill's own embedded
# repo plus per-run outputs (critique snapshots, live config). Never commit it.
# Root-anchored so it does NOT match the intentional apps/web/.impeccable/design.json.
/.impeccable/
